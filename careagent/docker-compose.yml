version: "3.9"
services:
api:
build: .
command: uvicorn src.api:app --host 0.0.0.0 --port 8000 --reload
env_file: .env
ports:
- "8000:8000"
volumes:
- .:/app
# Optional: LiveKit server (use official image or managed LiveKit Cloud)
# livekit:
# image: livekit/livekit-server
# command: ["--dev"]
# ports:
# - "7880:7880"